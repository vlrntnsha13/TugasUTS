<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugasuts">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DataMatkulActivity"></activity>
        <activity android:name=".DataDosenActivity" />
        <activity android:name=".DaftarKRSActivity" />
        <activity android:name=".CrudKrsActivity" />
        <activity android:name=".CrudDosenActivity" />
        <activity android:name=".CrudMhsActivity" />
        <activity android:name=".CrudMatkulActivity" />
        <activity android:name=".DataMhsActivity" />
        <activity android:name=".HalamanDosenActivity" />
        <activity android:name=".HalamanAdminActivity" />
        <activity android:name=".HalamanMhsActivity" />
        <activity android:name=".HalamanLoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>